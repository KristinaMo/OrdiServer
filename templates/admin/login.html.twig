{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block titre %}Connexion{% endblock %}

{% block body %}
{% if error %}
   <div class="alert alert-danger"> Les informations sont incorrection </div>

{% endif %}

<div class="connexion">
<form action="{{path('connexion')}}" method="post">
<div>
  <label>login :</label>
  <input type="text" require name="_username" class="form-control">
  </div>
  <div class="form-group">
  <label>Password :</label>
  <input type="password" require name="_password" class="form-control">
  </div>
  <input type="submit" class="btn btn-pimary" value="S'identifier">
  </form>
  </div>

{% endblock %}