{% extends 'base.html.twig' %}

{% block title %}Hello ContactController!{% endblock %}

{% block body %}


 <div class="navbar">

 <input type="checkbox" id="nav-toggle">
  <div class="logo">
  <img src="/image/OrdiService.png">
  </div>
 <div class="links">
  <a href="{{path("ordi")}}">Accueil</a>
  <a href="{{path("catalogue")}}">Catalogue</a>
  <a href="{{path("contact")}}">Nous contacter</a>

      
   {% if not app.user %}

      <a href="{{path('connexion')}}">Connexion</a>
 <a href="{{path('inscription')}}">Inscription</a>

        
   {% else %}
   
  <a href="{{path('deconnexion')}}">Deconnexion</a> 
   
    {% endif %}

 </div>
 <label for="nav-toggle" class="icon-burger">
 <div class="line"></div>
 <div class="line"></div>
 <div class="line"></div>
  </label>
       
</div>

<section id="contact">
<div class="titre-catalogue">
   <h1> Nous contacter</h1>
   </div>
<div class="contact">
 <h2> Si vous avez des questions ou des problèmes, n'hésitez pas à nous contacter:</h2>
 <h3> ordiserver@gmail.com</h3>
 </div>
 </section>
{% endblock %}
