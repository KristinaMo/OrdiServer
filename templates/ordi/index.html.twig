{% extends 'base.html.twig' %}

{% block title %}OrdiService{% endblock %}

{% block body %}
<div class="nav">
 <input type="checkbox" id="nav-toggle">
 <div class="logo">
 <img src="/image/OrdiService.png">
 </div>
 <div class="links">
 <a href="{{path("ordi")}}">Accueil</a>
 <a href="{{path("catalogue")}}">Catalogue</a>
 <a href="{{path("contact")}}">Nous contacter</a>
      
       
 {% if not app.user %}

 <a href="{{path('connexion')}}">Connexion</a>
 <a href="{{path('inscription')}}">Inscription</a>
        
         
 {% else %}
          
 <a href="{{path('deconnexion')}}">Deconnexion</a>
       
 {% endif %}
  </div>
 <label for="nav-toggle" class="icon-burger">
 <div class="line"></div>
 <div class="line"></div>
 <div class="line"></div>
 </label>
       
 </div>
        

<section id="header">

<div class="title">
<h1>OrdiService</h1>
<h5>Trouve ton reparateur d'ordinateur</h5>
<a href="{{path("catalogue")}}" class="button">Découvrir</a>

</div>

<div class="ordi">
<img src="/image/ordi.png">
</div>

</section>

{% endblock %}
